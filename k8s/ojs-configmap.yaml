apiVersion: v1
kind: ConfigMap
metadata:
  name: ojs-config
  namespace: research-platform
  labels:
    app.kubernetes.io/name: ojs
    app.kubernetes.io/part-of: research-platform
data:
  OJS_BASE_URL: "http://localhost:8080" # overridden per-environment via Helm values
  OJS_DB_HOST: "mysql"
